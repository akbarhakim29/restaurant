<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bymankind.restaurant">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminActivity" />
        <activity android:name=".KokiActivity" />
        <activity android:name=".PramusajiActivity" />
        <activity android:name=".KasirActivity" />
        <activity android:name=".posisi.CreatePosisiActivity" />
        <activity android:name=".posisi.UpdatePosisiActivity" />
        <activity android:name=".posisi.DeletePosisiActivity" />
        <activity android:name=".posisi.ReadPosisiActivity"></activity>
    </application>

</manifest>